# Контекст збірки образу — корінь репозиторію (див. infra/web.Dockerfile).
# Сюди не потрапляє нічого, що образу не потрібне.

.git
.github
.claude
.idea
.vscode

node_modules
**/node_modules

# Артефакти збірок — образ будує їх сам
**/dist
**/.output
**/.nitro
**/.tanstack

# Нативні проєкти Capacitor (MER-50): образ — це вебсервер, збирати з них
# нічого. Всередині ще й лежить копія вебзбірки, тож без цього рядка контекст
# роздувається на кожну збірку образу.
apps/web/android
apps/web/ios

# Секрети — у образі їм не місце в жодному вигляді
**/.env
**/.env.local

# Документація
*.md
LICENSE
