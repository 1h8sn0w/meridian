//  @ts-check

/** Спільний формат для всього воркспейсу. Значення — з офіційного стартера TanStack Start. */

/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
}

export default config
